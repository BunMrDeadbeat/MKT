<div class="container mx-auto px-4 py-3 text-sm text-gray-600">
    <a href="{{ '/' }}" class="hover:text-primary">Inicio</a> > 
    <a href="#" class="hover:text-primary">{{ $product->category->name }}</a> > 
    <span class="text-primary">{{$product->name}}</span>
</div>